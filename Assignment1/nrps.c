#include<stdio.h>
#include<string.h>

int main()
{
	int k, n, len, i, j, count = 0;

	printf("Enter value of k:");
	scanf("%d",&k);

	printf("Enter value of n:");
	scanf("%d",&n);

	char str[k];

	printf("Enter a string %d character long:");
	scanf("%s",str);

	len = strlen(str);

	
